apiVersion: v1
kind: Secret
metadata:
  name: mi-app-secret
type: Opaque
stringData:
  # Valor de EJEMPLO. Nunca commitear secretos reales:
  # en produccion -> RBAC + cifrado at-rest + Azure Key Vault / Vault.
  DB_PASSWORD: "cambiar-en-runtime"
